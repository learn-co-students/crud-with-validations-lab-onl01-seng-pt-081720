<%= link_to "Back", songs_path %>
<h1><%= @song.title %></h1>
<h2><%= @song.artist_name %> - <%= @song.genre %></h2>
<h3>
    <% if @song.released %>
        Song released in:
    <% end %>
    <% if !!@song.release_year %>
        <%= @song.release_year %>
    <% end %>
</h3>
<%= link_to "Edit", edit_song_path(@song) %> - <%= link_to "Delete", song_path(@song), method: :delete, data: { confirm: "Are You Sure You Want To Delete This Song?" }  %>