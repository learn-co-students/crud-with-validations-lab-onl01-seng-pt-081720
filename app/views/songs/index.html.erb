<h1>Songs</h1>
<% @songs.each do |song| %>
<div class="song">
  <span><%= song.title %></span>
    <tbody>
        <tr>
            <%= link_to "Delete", song, method: :delete, data: { confirm: "Are you sure?" } %>
        </tr>
    </tbody>
</div>
<% end %>


